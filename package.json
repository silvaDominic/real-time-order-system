{
  "name": "real-time-order-system",
  "version": "1.0.0",
  "author": "Dominic Silva",
  "description": "An applet that generates and renders food orders to a feed in real time. ",
  "workspaces": [
    "src/client",
    "src/server"
  ],
  "scripts": {
    "dev": "npm run dev --prefix src/client & npm start --prefix src/server",
    "build": "npm run build --prefix src/client",
    "start": "node src/server/server.js"
  },
  "dependencies": {
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "vite": "^5.4.8",
    "@vitejs/plugin-react": "^4.3.2"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/silvaDominic/real-time-order-system.git"
  }
}
